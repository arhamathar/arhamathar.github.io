// Typing Effect

var typed = new Typed(".typing",{
  strings:["Arham Athar","Web Developer."],
  typeSpeed: 50,
  backSpeed: 50,
  loop:true
});

// Typing Effect

// Fixed NavBar

$(window).scroll(function(){
	$('nav').toggleClass('scrolled', $(this).scrollTop() > 500);
});

// Fixed NavBar

// Scroll Animation

AOS.init({
  duration:1200,
});

//Scroll Animation

//lightbox
    lightbox.option({
      'resizeDuration': 500,
      'wrapAround': true
    })
//lightbox
